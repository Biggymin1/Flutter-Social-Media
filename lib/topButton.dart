import 'package:flutter/material.dart';
import 'body.dart' as body;

double radius = 25;

/*
class topButton extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Row(children: [leftTopButton(),middleTopButton(),rightTopButton()],
    mainAxisAlignment: MainAxisAlignment.spaceEvenly,);
  }
}

class leftTopButton extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return RaisedButton(
        color: Colors.orange,
        child: Text("Friends",style: TextStyle(color: Colors.white,fontWeight: FontWeight.bold),),
        padding: EdgeInsets.fromLTRB(10, 15, 10, 15),
        onPressed: () => {clearList()},
        splashColor: Colors.white,
        shape: RoundedRectangleBorder(
            side: BorderSide(color: Colors.white),
            borderRadius: BorderRadius.circular(radius)));
  }
}

class rightTopButton extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return RaisedButton(
        color: Colors.red,
        child: Text("Explore",style: TextStyle(color: Colors.white,fontWeight: FontWeight.bold),),
        onPressed: () => {clearList()},
        splashColor: Colors.white,
        padding: EdgeInsets.fromLTRB(10, 15, 10, 15),
        shape: RoundedRectangleBorder(
            side: BorderSide(color: Colors.white,),
            borderRadius: BorderRadius.circular(radius)));
  }
}

class middleTopButton extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return RaisedButton(
        color: Colors.green,
        splashColor: Colors.white,
        child: Text("Solve",style: TextStyle(color: Colors.white,fontWeight: FontWeight.bold),),
        padding: EdgeInsets.fromLTRB(10, 15, 10, 15),
        onPressed: () => {clearList()},
        shape: RoundedRectangleBorder(
            side: BorderSide(color: Colors.white),
            borderRadius: BorderRadius.circular(radius)));
  }
}
 */